const Footer = () => {
  return (
    <footer style={{ backgroundColor: "#333", padding: "10px", color: "#fff", textAlign: "center" }}>
      <p>&copy; {new Date().getFullYear()} Motel Booking. Todos os direitos reservados.</p>
    </footer>
  );
};

export default Footer;
